# HTTP on port 80 - main access point (recommended for IP access)
:80 {
    reverse_proxy message-correction-api:80
}

# HTTPS on port 443 - with basic self-signed cert
:443 {
    tls internal
    reverse_proxy message-correction-api:80
}
